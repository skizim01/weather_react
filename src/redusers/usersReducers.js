const initialState = {
user:[
    {name:"Stepan", id:1, follow: false}
]
}

const usersReducers = (state = initialState, action) =>{
    switch (action.type){
        case "FOLLOW_UNFOLLOW":
            if (state.user.follow === true){

            }



                }


}
